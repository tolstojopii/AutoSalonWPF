<Window x:Class="AutoServiceWPF.Views.SalesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Продажи (услуги)" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Список заказов услуг (продаж)" FontSize="16" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>

        <DataGrid x:Name="SalesDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID заказа" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Client.FIO}" Width="200"/>
                <DataGridTextColumn Header="Услуга" Binding="{Binding Service.Title}" Width="150"/>
                <DataGridTextColumn Header="Стоимость услуги" Binding="{Binding Service.Cost, StringFormat={}{0:C}}" Width="120"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding Starttime, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Обновить" Width="80" Height="30" Margin="5" Click="RefreshButton_Click"/>
            <Button Content="Закрыть" Width="80" Height="30" Margin="5" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>