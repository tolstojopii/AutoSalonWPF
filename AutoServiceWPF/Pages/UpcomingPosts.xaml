<Page x:Class="AutoServiceWPF.Pages.UpcomingPosts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AutoServiceWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UpcomingPosts">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="5" Text="Лучший автосервис" Foreground="White" FontFamily="Comic Sans MS" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24"/>
                    <Image HorizontalAlignment="Right" VerticalAlignment="Center" Source="/Pages/Preview.jpg" Width="100"/>
                </StackPanel>
            </StackPanel>
            <Button Content="Назад" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="16"/>
        </Grid>
        <DataGrid Background="White" Grid.Row="1" Name="DataGridRecords" IsReadOnly="True" AutoGenerateColumns="False"> 
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование услуги" Binding="{Binding Service.Title}" Width="*"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding Client.FIO}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Client.Email}" Width="*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Client.Phone}" Width="*"/>
                <DataGridTextColumn Header="Дата и время записи" Binding="{Binding StartTime, StringFormat=dd.MM.yyyy HH:mm}" Width="*"/>
                <DataGridTemplateColumn Width="*" Header="Оставшееся время">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TotalTime}" Foreground="{Binding ColorOfTime}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
